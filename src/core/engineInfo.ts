/**
 * The engine's own version. Providers declare an engineCompatibility semver
 * range; the validator checks against this. Bump on breaking provider-contract
 * changes only.
 */
export const ENGINE_VERSION = '1.0.0';
